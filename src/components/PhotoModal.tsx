// Composant PhotoModal : fenêtre contextuelle pour afficher les photos avec carrousel
import React from "react";

const PhotoModal: React.FC = () => {
  return <div>Section en cours de développement</div>;
};

export default PhotoModal;
